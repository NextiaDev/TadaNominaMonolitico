@model TadaNomina.Models.ViewModels.Catalogos.ModelImpuestoSat

@{
    ViewBag.Title = "CreateLayout";
    Layout = "~/Views/Shared/_layoutPrincipal.cshtml";
}


<div id="page-head">
    <div id="page-title">
        <h1 class="page-header text-overflow">Nuevo Layout Impuestos SAT</h1>
    </div>
    <div id="rutaPrincipal"></div>
</div>

<div id="page-content">
    <div class="col-md-8 col-md-offset-2">
        <div class="panel">
            <div class="panel-heading">
                <h3 class="panel-title">Carga Layout</h3>
            </div>
            <div class="panel-body">
                @using (Html.BeginForm("CreateLayout", "ImpuestoSat", FormMethod.Post, new { enctype = "multipart/form-data" }))
                {
                    @Html.AntiForgeryToken()

                    <div class="form-horizontal">

                        <hr />
                        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

                        <div class="form-group">
                            @Html.Label("Descargar Layout:", new { @class = "control-label col-md-3" })
                            <div class="col-md-9">
                                <a href="~/Formatos/LayoutImpuestoSat.xlsx" download="LayoutImpuestoSat">
                                    <span class="fa fa-cloud-download"> Layout Impuesto SAT</span>
                                </a>
                            </div>
                        </div>
                        <div class="form-group">
                            @Html.LabelFor(model => model.Archivo, htmlAttributes: new { @class = "control-label col-md-3" })
                            <div class="col-md-9">

                                @Html.TextBoxFor(model => model.Archivo, new { type = "file", @class = "inputfile" })
                                @Html.ValidationMessageFor(model => model.Archivo, "", new { @class = "text-danger" })
                            </div>
                        </div>
                        <br />
                        <div class="form-group">
                            <div class="col-md-12">
                                @if (Model.Mensaje != null)
                                {
                                    if (Model.validacion)
                                    {
                                        <div class="alert alert-info">
                                            @Model.Mensaje
                                        </div>
                                    }
                                    else
                                    {
                                        <div class="alert alert-danger">
                                            @Model.Mensaje
                                        </div>
                                    }
                                }
                            </div>
                        </div>
                        <br />
                        <div class="panel-footer text-right">
                            <div class="form-group">
                                <div class="col-md-offset-2 col-md-10">
                                    <input type="submit" value="Cargar" class="btn btn-primary" />
                                    @Html.ActionLink("Regresar a lista", "Index", null, new { @class = "btn btn-info" })
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>